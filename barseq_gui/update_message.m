function update_message(app,message)
% Update messagearea in app. 
% app.MessageTextArea.Value=[app.MessageTextArea.Value(:)', {sprintf('Creating local designated maxproj folder=%s', destpath)}];
% app.MessageTextArea.Value{4}='All hard drives have enough space for current cycle!';